{{template "base" .}}

{{define "title"}}About Page{{end}}

{{define "content"}}
   <div class="container">
        <div class="row">
            <div class="col">
                <h1>This is about page</h1>
                <p>This come from templates: {{index .StringMap "test"}}</p>
            </div>
        </div>
    </div>
{{end}}